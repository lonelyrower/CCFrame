# 优化npm安装配置
audit=false
fund=false

# 使用更快的registry
registry=https://registry.npmjs.org/

# 禁用可选依赖以减少警告
optional=false

# 使用更严格的引擎检查
engine-strict=true

# 减少日志噪声
loglevel=warn

# 禁用更新检查通知
update-notifier=false