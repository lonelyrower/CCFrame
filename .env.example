# ============================================
# CCFrame Environment Configuration
# ============================================
#
# For Docker Compose deployment:
# - Copy this file to `.env` and update the values
# - DATABASE_URL is automatically set in docker-compose.yml
# - Only set POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB below
#
# For local development:
# - Copy this file to `.env.local`
# - Set DATABASE_URL to your local PostgreSQL instance
# ============================================

# Database Configuration (for Docker Compose)
# These are used by docker-compose.yml to configure PostgreSQL
POSTGRES_USER=ccframe
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=ccframe

# Database URL (for local development only)
# For Docker: This is automatically set in docker-compose.yml, do not set here
# For local dev: postgresql://user:password@localhost:5432/ccframe?schema=public
# DATABASE_URL="postgresql://user:password@localhost:5432/ccframe?schema=public"

# Next Auth
NEXTAUTH_SECRET="your-nextauth-secret-change-this-to-a-random-string"
NEXTAUTH_URL="http://localhost:3000"

# Admin credentials (for initial setup)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="change-this-password"

# Base URL (change to your domain in production)
BASE_URL="http://localhost:3000"

# Remote image hosts (comma-separated). Defaults to Cloudflare Images.
NEXT_PUBLIC_IMAGE_HOSTS="imagedelivery.net"
